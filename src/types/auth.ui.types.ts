export type CardWrapperPropType = {
  label: string;
  title: string;
  backButtonHref: string;
  backButtonLabel: string;
  children: React.ReactNode;
};

export type AuthHeaderPropType = {
  label: string;
  title: string;
};

export type BackButtonPropType = {
  label: string;
  href: string;
};
